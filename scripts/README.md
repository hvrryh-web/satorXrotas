# scripts/

Bash + PowerShell setup scripts. Bash is canonical; PowerShell mirrors are kept for Windows-only contributors.

```
setup.sh    Install deps, copy .env.example, run typecheck
setup.ps1   Windows equivalent
check.sh    Run lint + typecheck + tests + doc-tier:check
```
