# infra/

Infrastructure-as-code and deploy configuration.

```
vercel/    Per-app Vercel deploy config (site, web)
render/    Render service config (rat-os-api, Phase 2+)
docker/    docker-compose for local dev parity
```

See `docs/operations/DEPLOYMENT.md` and `docs/operations/ENVIRONMENTS.md`.
