# .agents/phase-logbooks/

Per-phase chronological logbooks. Filename `PHASE-<N>-LOGBOOK.md`.

Each phase opens with an entry on unlock and closes with an entry on completion (including exit-criteria evidence). Intermediate entries are optional but encouraged for any decision that doesn't merit a full ADR but does merit a record.

Logbooks are T1 — load on demand.
