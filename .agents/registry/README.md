# .agents/registry/

Agent registry — *which* agent is in which role for this repo, and contact info / scope for each.

```
AGENT_REGISTRY.md          # Live list of registered agents and their roles
```

Updated when an agent is onboarded, role-reassigned, or off-boarded.
