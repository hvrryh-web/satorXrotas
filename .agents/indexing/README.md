# .agents/indexing/

Document filter rules. The tier system (`.doc-tiers.json`) says *whether* to load. The rules here say *what to extract* once loaded — useful for agents with small context windows that need to summarise.

```
FILTER_RULES.md  # Heuristics for selectively reading large docs
```
