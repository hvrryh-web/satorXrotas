# .agents/workflows/

Declarative agent workflows, YAML/JSON. Pair with `.agents/COLLABORATION_RUNBOOK.md` (human-readable runbooks) — workflows here are the machine-readable form, callable from agent tooling.

Examples (to be filled):

- `bootstrap-module.yaml` — Runbook 1 codified
- `add-adr.yaml` — Runbook 2 codified
- `open-gate.yaml` — Runbook 4 codified
