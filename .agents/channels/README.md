# .agents/channels/

Channel registry. The authoritative list lives in `index.json`. The descriptive prose in `COORDINATION_PROTOCOL.md` is derived from it.

```
index.json    # Channel definitions: { name, owner_role, paths, gate }
```
