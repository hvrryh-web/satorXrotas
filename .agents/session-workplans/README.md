# .agents/session-workplans/

Long-form workplans spanning multiple sessions. Filename `SW-YYYYMMDD-<slug>.md`.

Use this when:

- A task spans 2+ sessions.
- Multiple agents will pick up the same workstream.
- The work needs explicit decomposition and sequencing.

Cross-link from `.agents/active/<branch>.md`.
