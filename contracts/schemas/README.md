# contracts/schemas/

JSON schemas shared between RAT-OS and upstream services beyond what `openapi/` and `events/` cover. Currently empty; populated as new shared types emerge.

Naming: `<concept>.schema.json`.
