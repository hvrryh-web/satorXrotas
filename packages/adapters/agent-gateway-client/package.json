{
  "name": "@njz-os/adapters-agent-gateway-client",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "NJZ RAT-OS adapter — agent-gateway-client (thin client into upstream service)",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "lint": "eslint src --max-warnings=0",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "@njz-os/core": "workspace:*",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@njz-os/tsconfig": "workspace:*",
    "msw": "^2.6.0",
    "typescript": "^5.6.0",
    "vitest": "^2.0.0",
    "eslint": "^9.0.0"
  }
}
