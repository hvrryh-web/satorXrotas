{
  "name": "@njz-os/adapters-vaultbrain-client",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "NJZ RAT-OS adapter — vaultbrain-client (thin client into upstream service)",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "lint": "eslint src --max-warnings=0",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "@njz-os/core": "workspace:*",
    "zod": "^3.23.0",
    "idb-keyval": "^6.2.1"
  },
  "devDependencies": {
    "@njz-os/tsconfig": "workspace:*",
    "msw": "^2.6.0",
    "typescript": "^5.6.0",
    "vitest": "^2.0.0",
    "eslint": "^9.0.0"
  }
}
