{
  "name": "@njz-os/progression",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "NJZ RAT-OS — progression",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "lint": "eslint src --max-warnings=0",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "@njz-os/core": "workspace:*"
  },
  "devDependencies": {
    "@njz-os/tsconfig": "workspace:*",
    "typescript": "^5.6.0",
    "vitest": "^2.0.0",
    "eslint": "^9.0.0"
  }
}
