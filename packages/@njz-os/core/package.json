{
  "name": "@njz-os/core",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "NJZ RAT-OS core domain types — pure TypeScript, no I/O",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "lint": "eslint src --max-warnings=0",
    "clean": "rm -rf dist .turbo"
  },
  "devDependencies": {
    "@njz-os/tsconfig": "workspace:*",
    "typescript": "^5.6.0",
    "vitest": "^2.0.0",
    "eslint": "^9.0.0"
  }
}
