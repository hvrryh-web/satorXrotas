{
  "name": "@njz-os/focus-engine",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "NJZ RAT-OS — focus-engine",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "lint": "eslint src --max-warnings=0",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "@njz-os/core": "workspace:*"
  },
  "devDependencies": {
    "@njz-os/tsconfig": "workspace:*",
    "typescript": "^5.6.0",
    "vitest": "^2.0.0",
    "eslint": "^9.0.0"
  }
}
