# @njz-os/tsconfig

Shared TypeScript config presets.

```jsonc
// In a package's tsconfig.json
{
  "extends": "@njz-os/tsconfig/base.json"   // for non-React packages
  // or
  "extends": "@njz-os/tsconfig/react.json"  // for React packages
}
```
